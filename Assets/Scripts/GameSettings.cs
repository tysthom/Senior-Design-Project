// GameSettings.cs
public static class GameSettings
{
    // Set this to true if this instance should run as the server.
    public static bool IsServer = false;

    // For client instances, store the chosen team ("Red" or "Blue").
    public static string Team = "";
}
